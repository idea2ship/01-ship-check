# Gemini
GEMINI_API_KEY=
# Optional: override the model name (default: gemini-2.5-flash)
GEMINI_MODEL=

# Supabase (server-side only — never expose to the client)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
